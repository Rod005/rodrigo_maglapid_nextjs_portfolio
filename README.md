# Rodrigo Maglapid Portfolio V4 — Layout Fixed

This update keeps Version 4 and corrects the project showcase and responsive device presentation.

## Fixes
- Four balanced project cards on wide desktop screens
- Two cards on tablets and one card on mobile
- Project images use `object-contain` so dashboards are not cropped
- Consistent card height, spacing, labels and action links
- Redesigned desktop, laptop, tablet and phone showcase
- Device images stay inside their frames at all screen sizes

Upload all files to the existing GitHub repository and commit to `main`. Vercel will deploy automatically.
